FROM busybox:ubuntu

WORKDIR /opt/lightning-webserver
# Copy files from deploy/ directory into image's working directory
COPY . /opt/lightning-webserver

RUN tar -xf lightning.tar

EXPOSE 8080:8080

CMD ["./lightning", "simple_config"]
