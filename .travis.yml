# Ubuntu 14.04 to use newer Boost version
os:
  - linux
dist: trusty
sudo: required

compiler:
  - gcc

addons:
  apt:
    packages:
      - libboost-all-dev
      - httpie

before_script: 
  - chmod 755 configure.sh
  - ./configure.sh

script: make && make test && make clean
