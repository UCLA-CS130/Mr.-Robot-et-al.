# Ubuntu 14.04 to use newer Boost version
os:
  - linux
dist: trusty
sudo: required

compiler:
  - gcc

addons:
  apt:
    packages:
      - libboost-all-dev
      - httpie
      - lcov

script: make && make test && make clean
